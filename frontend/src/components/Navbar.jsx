// src/components/Navbar.jsx
export function Navbar({ domain, onDomainChange }) {
  const domains = ['Air','Land','Sea'];
  return (
    <nav>…buttons for each domain calling onDomainChange(domain)…</nav>
  );
}
